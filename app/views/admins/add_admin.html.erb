
<div class="container" style="max-width: 300px;">

<% if admin_signed_in? %>
<strong>-> Authenticated as: <%=current_admin.email%> </strong>

<%= render 'add_admin_form' %> 

<% else %>
<strong>This action will not allowed because it requires Admin rights.</strong>
<% end %>

<h3> <%= link_to 'Back', :root, :class => "btn btn-default btn-block", :style => 'margin-top: 5px;' %> </h3>

</div>