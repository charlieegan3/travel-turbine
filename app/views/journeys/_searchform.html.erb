<%= form_for @journey do |f| %>
  <%= f.label :origin, 'Origin' %>:
  <%= f.collection_select :origin, @origin, :id, :name, :prompt => true %><br />
  <%= f.label :destination, 'Destination' %>:
  <%= f.collection_select :origin, @origin, :id, :name, :prompt => true %><br />
  <%= f.label :date, 'Date' %>:
 <!-- PICK DATE WITH A DROP-DOWN OR JQUERY CALENDAR -->
  <%= f.submit %>
<% end %>
